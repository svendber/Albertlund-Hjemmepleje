@model IEnumerable<Albertlund_Hjemmepleje.Models.Entities.Log>
@{
    ViewBag.Title = "Log";
}

<h2>Log</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelIteam => item.Time)
        </td>
    </tr>
}
</table>